{
  "name": "jenkins-pipeline",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha test",
    "coverage": "istanbul cover _mocha test"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "istanbul": "^0.4.5"
  }
}
