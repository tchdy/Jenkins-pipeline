{
  "name": "jenkins-pipeline",
  "version": "1.0.0",
  "scripts": {
    "test": "npx mocha test",
    "coverage": " npx istanbul cover _mocha test"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "istanbul": "^0.4.5"
  }
}
